export class ProjectClass {
    // Constructors

    constructor(id, name){
        this.id = id;
        this.name = name;
    };

    constructor(){
        this.id = "default";
        this.name = "default";
        };
}